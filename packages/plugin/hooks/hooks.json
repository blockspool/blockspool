{
  "hooks": [
    {
      "event": "Stop",
      "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/hook-driver.js stop",
      "description": "BlockSpool loop driver — blocks exit while session is active"
    },
    {
      "event": "PreToolUse",
      "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/hook-driver.js PreToolUse",
      "description": "BlockSpool scope enforcement — blocks out-of-scope file writes"
    }
  ]
}
